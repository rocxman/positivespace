{
  "name": "positivespace",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "start": "cd apps/api && uvicorn app.main:app --host 0.0.0.0 --port $PORT"
  },
  "devDependencies": {
    "turbo": "^2.0.0"
  }
}
